export enum SpeedCalculatorMode {
  StatsAndMeta = "Stats and Meta",
  Stats = "Stats",
  Meta = "Meta",
  Base = "Base"
}

export const SPEED_CALCULATOR_MODES: string[] = Object.values(SpeedCalculatorMode)
